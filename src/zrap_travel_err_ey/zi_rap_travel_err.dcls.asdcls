@EndUserText.label: 'Access control for ZI_RAP_TRAVEL_ERR'
@MappingRole: true
define role ZI_RAP_TRAVEL_ERR {
    grant
        select
            on
                ZI_RAP_TRAVEL_ERR
                    where
//                    ( TravelStatus ) = aspect pfcg_auth ( ZOSTATERR, ZOSTATERR,  actvt = '03') and
//                    CurrencyCode     = 'EUR';
                    true;
                        
}